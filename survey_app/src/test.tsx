function Message(){     
    const name = 'Testing';
    if (name)
        return <h1>Hello World, this is {name}</h1> // jsx = javascript xml }
    else
        return <h1>12345 </h1>
}

export default Message;

// virtual DOM -> changes